# 이 파일은 도메인별로 분리되었습니다.
# 각 라우터 파일을 직접 참조하세요:
#   analysis.py   — /summary, /comparison, /score, /facilities
#   listings.py   — /listings/search, /listings/all, /listings/{id}
#   market.py     — /market/stats, /market/trend
#   recommend.py  — /recommend
#   favorites.py  — /favorites (CRUD)
#   admin.py      — /admin/reset/{table}
